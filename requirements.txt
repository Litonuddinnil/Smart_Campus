# Runtime dependencies only -- kept minimal for a fast, free CPU-only build
# (Hugging Face Spaces free tier / Docker fallback).
Flask==3.1.3
numpy==2.4.4
scipy==1.17.1
requests==2.33.1
gunicorn==23.0.0
python-dotenv==1.2.2
